# VASTRA — Claude Code Rules
- NEVER create git branches. Push to main only.
- NEVER modify color values in constants/theme.ts — they are LOCKED.
- TypeScript strict mode. Zero errors always.
- Font: Inter only (via @expo-google-fonts/inter).
- Background is ALWAYS #000000.
- All surfaces use glass morphism (rgba white overlays, never solid backgrounds).
- Test on Android via Expo Go.
